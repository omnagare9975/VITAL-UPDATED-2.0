# Frontend is deployed on AWS S3 — this Dockerfile is NOT used in production.
# Run:  npm run build   then upload the /build folder to S3.
# See deploy-s3.sh in the repo root for the full S3 deploy script.
